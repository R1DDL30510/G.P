# Configuration overrides for GARVIS stack
# Copy to config/env and adjust as needed.

# Base port for first GPU-backed Ollama instance
GPU_PORT_BASE=11434

# Router service port
ROUTER_PORT=28100

# Evaluator proxy port
EVAL_PORT=11437

# Path to Ollama binary (uncomment to override auto-detection)
# OLLAMA_BIN=/usr/bin/ollama
