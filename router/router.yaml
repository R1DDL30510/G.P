server:
  host: 0.0.0.0
  port: 28100
  request_timeout_s: 120
  connect_timeout_s: 5
  read_timeout_s: 120

targets:
  gpu0: { url: "http://127.0.0.1:11434", tags: ["gpu","fast"] }
  gpu1: { url: "http://127.0.0.1:11435", tags: ["gpu","balanced"] }
  cpu:  { url: "http://127.0.0.1:11436", tags: ["cpu","fallback"] }

routing:
  default: gpu0
  by_keyword:
    "vision|image|ocr": gpu1
    "json|schema|validate": cpu
  by_model_alias:
    fast: gpu0
    balanced: gpu1
    safe: cpu
